<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"

    package="com.maliatecpharm">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="My First App"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        tools:replace = "android:label"
        android:theme="@style/Theme.InventoryManagement">

        <activity
            android:name=".activity.mainmenu.activities.ActivityLogIn"
            android:label="@string/title_activity_main_menu"
            android:theme="@style/Theme.InventoryManagement.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.mainmenu.activities.ActivityProfile"
            android:label="My first App"
            android:theme="@style/Theme.InventoryManagement.NoActionBar" />

        <activity
            android:name=".activity.mainmenu.activities.MainMenuActivity"
            android:label="@string/title_activity_main_menu"
            android:theme="@style/Theme.InventoryManagement.NoActionBar">
        </activity>



        <activity android:name=".activity.mainmenu.activities.ActivityMedications" />

        <activity android:name=".activity.mainmenu.activities.ActivityRegisterPage" />

        <activity android:name=".activity.mainmenu.activities.ActivityMedFriend" />
        <activity android:name=".activity.mainmenu.activities.ActivitySettings" />
        <activity android:name=".activity.mainmenu.activities.ActivityFeedback" />
        <activity android:name=".activity.mainmenu.activities.ActivityVitalSigns" />
        <activity android:name=".activity.mainmenu.database.RecyclerViewProfiles" />
        <activity android:name=".activity.mainmenu.activities.ActivityProfiles" />


    </application>

</manifest>