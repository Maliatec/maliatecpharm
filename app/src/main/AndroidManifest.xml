<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.maliatecpharm">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="My First App"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InventoryManagement">

        <activity
            android:name=".activity.LogInActivity"
            android:label="@string/title_activity_main_menu"
            android:theme="@style/Theme.InventoryManagement.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.Profile"
            android:label="My first App"
            android:theme="@style/Theme.InventoryManagement.NoActionBar" />

        <activity
            android:name=".activity.mainmenu.MainMenuActivity"
            android:label="@string/title_activity_main_menu"
            android:theme="@style/Theme.InventoryManagement.NoActionBar">
        </activity>

        <fragment
            android:name =".fragment.HomeFragment"
            android:label ="MainActivity"

            ></fragment>

        <activity android:name=".activity.Medications" />
        <activity android:name=".activity.mainmenu.Home" />
        <activity android:name=".activity.More" />
        <activity android:name=".activity.mainmenu.Doctors" />
        <activity android:name=".activity.mainmenu.RegisterPage" />
        <activity android:name=".activity.mainmenu.AddMedications" />


    </application>

</manifest>